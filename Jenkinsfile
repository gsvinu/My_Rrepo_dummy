import groovy.json.JsonOutput
import hudson.util.Secret
pipeline{
	agent any
	stages {
		stage('Build Config') {
			steps{
				echo "build stage"
			}
		}
		stage('Git Checkout') {
			steps {
				echo "git stage"
			}
		}
	}
}	
